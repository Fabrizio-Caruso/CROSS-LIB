In plain ANSI C, with no heap, with just uint8_t and uint16_t types for integers, with only Cross-Lib APIs for input and output as described in the attachment,

...

Do not use _XL_DRAW or _XL_DELETE on tiles that have not changed.
